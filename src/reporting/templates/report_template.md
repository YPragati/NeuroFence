# NeuroFence Security Testing Report

**Run ID:** {{ run_id }}
**Generated:** {{ generated_at }}

---

## 1. Executive Summary

This report summarizes an automated AI security testing session run
by NeuroFence against a **local, fully controlled toy model**. All
tests -- adversarial prompt fuzzing, simulated backdoor trigger
testing, ML-based anomaly detection, activation-feature analysis,
and integrated risk scoring -- were executed against locally
controlled or explicitly authorized targets only.

- **Total prompts in dataset:** {{ total_prompts }}
- **Total fuzz mutation runs:** {{ total_fuzz_runs }}
- **Total simulated backdoor tests:** {{ total_backdoor_tests }}
- **Security score (share LOW/MEDIUM risk):** {{ security_score }} / 100

---

## 2. Test Configuration

{{ test_configuration }}

---

## 3. Testing Methodology

1. A labeled prompt dataset was built across 4 base categories (normal,
   adversarial, malicious-pattern, trigger); additional edge and
   random prompts were generated by the seeded fuzzer (Module 3).
2. Each prompt was mutated using {{ mutation_type_count }} distinct
   mutation strategies (case swap, character noise, synonym wrapping,
   injection wrapping, encoding hints, whitespace padding, and
   unicode-confusable substitution).
3. Synthetic backdoor triggers were injected into a subset of prompts
   and compared against trigger-stripped ("clean") counterparts to
   test whether the target model's behavior changed only when the
   trigger was present.
4. Behavior scores (consistency, prompt similarity, confidence proxy)
   were computed for every test run (Module 5).
5. Three ML anomaly detection algorithms (Isolation Forest, One-Class
   SVM, Local Outlier Factor) were run on the behavior score features
   and compared (Module 6).
6. Activation features were collected for every fuzz/backdoor execution
   and compared against a normal-activation baseline to produce
   activation anomaly scores (Module 6b).
7. Activation anomaly, injection, trigger, and response-change signals
   were combined via a weighted risk scorer into a 0--100 risk score
   with LOW / MEDIUM / HIGH / CRITICAL severity levels (Module 6c).
8. Detection metrics (precision, recall, F1, accuracy, false positive/negative
   rate, coverage) were computed against known heuristic/backdoor
   ground truth labels (Module 7).

---

## 4. Test Case Summary

| Category | Prompt Count |
|---|---|
{{ category_table }}

---

## 5. Backdoor Testing Findings

{{ backdoor_findings }}

---

## 6. Anomaly Detection Results

{{ anomaly_results_table }}

---

## 7. Security Risk Distribution

{{ risk_distribution }}

---

## 8. Security Evaluation Metrics

{{ metrics_table }}

> **Note on ML metrics:** The F1 / accuracy figures above measure how
> well each ML detector agrees with NeuroFence's own heuristic
> ground truth, not an absolute real-world detection rate. These
> detectors serve as supporting cross-checks; the deterministic
> trigger-firing (Module 4) and risk-scoring (Module 6c) stages
> are the primary signals in this simulated pipeline.

---

## 9. Suspicious Cases

{{ suspicious_cases }}

---

## 10. Recommendations

{{ recommendations }}

---

## 11. Limitations

{{ limitations }}

---

## 12. Conclusion

{{ conclusion }}

---

*This report was generated automatically by NeuroFence. All backdoor
and trigger tests were performed against a simulated, locally
controlled toy model for research/educational purposes. Results
labeled as "simulated" should not be interpreted as findings against
any real, deployed AI system.*
